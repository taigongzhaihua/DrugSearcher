<UserControl x:Class="DrugSearcher.Views.Controls.HotKeyInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:DrugSearcher.Converters"
             mc:Ignorable="d"
             d:DesignHeight="32" d:DesignWidth="200"
             Focusable="True"
             IsTabStop="True">
    <UserControl.Resources>

        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!-- 自定义样式 -->
        <Style x:Key="HotKeyBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BorderControlBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4,6,4" />
            <Setter Property="MinWidth" Value="160" />
            <Setter Property="Focusable" Value="True" />
            <Setter Property="CornerRadius" Value="4"/>

            <Style.Triggers>
                <!-- 焦点状态 -->
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderControlHoverBorderBrush}" />
                    <Setter Property="Effect" Value="{StaticResource HoverShadowEffect}"/>
                </Trigger>
                <!-- 录制状态 - 使用DataTrigger绑定到UserControl的属性 -->
                <DataTrigger Binding="{Binding IsRecording, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderControlPressedBorderBrush}" />
                    <Setter Property="Background" Value="{DynamicResource BorderControlPressedBackgroundBrush}"/>
                    <Setter Property="Effect" Value="{StaticResource HoverShadowEffect}"/>
                </DataTrigger>
                <!-- 悬停状态 -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderControlHoverBorderBrush}" />
                    <Setter Property="Background" Value="{DynamicResource BorderControlHoverBackgroundBrush}" />
                    <Setter Property="Effect" Value="{StaticResource HoverShadowEffect}"/>
                </Trigger>
                <!-- 禁用状态 -->
                <DataTrigger Binding="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                    <Setter Property="Background" Value="{DynamicResource BorderControlDisabledBackgroundBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderControlDisabledBorderBrush}" />
                    <Setter Property="Opacity" Value="0.6"/>
                    <Setter Property="Effect" Value="{x:Null}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- 清除按钮样式 -->
        <Style x:Key="ClearButtonStyle" TargetType="Button" BasedOn="{StaticResource TransparentThemeButtonStyle}">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="FontSize" Value="18"></Setter>
            <Setter Property="Foreground" Value="{DynamicResource TertiaryTextBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border x:Name="MainBorder" Style="{StaticResource HotKeyBorderStyle}"
            KeyboardNavigation.IsTabStop="True">

        <Grid>
            <TextBlock x:Name="DisplayTextBlock"
                       Text="{Binding DisplayText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       TextTrimming="CharacterEllipsis"
                       Visibility="{Binding IsRecording, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />

            <TextBlock Text="请按下快捷键..."
                       Foreground="{DynamicResource PrimaryBrush}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       FontStyle="Italic"
                       Visibility="{Binding IsRecording, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation 
                                    Storyboard.TargetProperty="Opacity"
                                    From="0.5" To="1" Duration="0:0:0.6"
                                    RepeatBehavior="Forever"
                                    AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>

            <Button x:Name="ClearButton"
                    Style="{StaticResource ClearButtonStyle}"
                    Content="&#xe60f;"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    FontSize="14" 
                    Visibility="{Binding HasValue, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Click="ClearButton_Click"
                    ToolTip="清除快捷键"
                    Focusable="False" />
        </Grid>
    </Border>
</UserControl>